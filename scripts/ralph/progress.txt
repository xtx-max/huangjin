# Ralph Progress Log
Started: 2026-08-14 12:00:03 +0800
---

## 共享上下文（每个迭代先读这里）
- 项目：GPAFP 静态化改造的黄金行情分析站（详见仓库根 AGENTS.md，务必先读）
- 前端目录：frontend/；构建验证：cd frontend && npm run build（vue-tsc 必须零错误）
- 依赖已安装（node_modules 存在）；Python 脚本仅标准库，用 python3
- 数据源已验证可用（见 AGENTS.md）；tushare token 在仓库根 .env（gitignored，禁止提交/泄露）
- 事件库/新闻为中文人工撰写内容，事实必须真实（日期、事件名、金价反应）
- UI 验证：npm run dev 起 vite（默认 5173 端口），用 dev-browser skill 检查
- 每次迭代只实现一个故事；完成后 commit: feat: US-0XX - 标题

## 已实测事实（2026-08-14）
- tushare `news`（新闻快讯）接口：当前 token 返回 code=40203 无权限（新闻类接口需单独开通，与积分无关）。
  US-009 的 fetch_news.py 必须按"权限不足→打印提示→退出码0→不覆盖 news.json"路径实现；新闻内容以人工维护的静态 news.json 为主。
- tushare `sge_daily`（Au99.99）：code=0 可用，数据始于 2004-01-02，单次最多 2000 行，需按日期分页。
- tushare `sge_basic` 可用；`fut_daily` 对 SHFE.AU0 返回 0 行（不用）。
- 国际日线数据文件 XAU_1d_data.csv（FeziweMelvin/XAUUSD-Gold-Price）覆盖 2004-06-11 至今、约 5400 行，分号分隔。

## 2026-08-14 12:5x — US-001 静态化改造基座 ✅（本轮由主会话直接实现）
- deepcode-ralph 运行器在本环境不可用（expect TTY 下 deepcode 的 bash 工具 spawn EBADF，会话卡死在提问），已弃用；后续故事由主会话按"一次一个故事"纪律直接实现并验证。
- 变更：删除 backend/、start*.ps1/bat；删除 Login/ChangePassword/UserManagement/Feedback/Settings/Monitor 页、auth/analysis 组件、useCaptcha、services/api.ts、store、utils；vite.config 移除 /api 代理；main.ts 移除 pinia；package.json 移除 axios/pinia/vue-quill/marked；App.vue 菜单改为 行情总览/历史数据/波动分析/黄金新闻。
- 验证：npm run build 零错误（vue-tsc）；dev 服务器根路径 HTTP 200；grep 无 axios|/api|localStorage|/login|createPinia 残留。
- 复用经验：四个页面目前是 el-empty 占位骨架，后续故事直接填充；Element Plus 中文 locale 已配置。

## 2026-08-14 — US-002 数据获取脚本与金价静态数据 ✅
- 新增 scripts/fetch_gold_data.py（仅标准库）；macOS python.org Python 需 certifi 做 SSL（已内置 fallback：certifi→系统默认，校验失败给中文指引，绝不禁用校验）。
- tushare sge_daily 分页陷阱：①窗口首日可能是周末导致"窗口最早>窗口起点"误判触底；②必须按"本窗口"最早日期判断而非全局。最终改为以 2004-01-01 硬下限逐 3 年窗口回溯，8 次调用拉全 5498 行。
- 数据事实：1970-01 国际金价 $35/盎司、2004-01-02 国内 Au99.99 ¥111.18/克，均与史实相符。
- 已知缺口：国际日线镜像（FeziweMelvin/XAUUSD-Gold-Price）尾端 2025-06-06，落后约 14 个月；国内 tushare 数据到 2026-08-13 实时。US-010 的 CI 定时刷新（Yahoo 在美国可用）或手动重跑脚本可补尾端。

## 2026-08-14 — US-003 首页行情总览 ✅
- 新增 src/data/goldPrices.ts：接口类型 + loadGoldPrices()，以 `import raw from './gold-prices.json?raw'` + JSON.parse 载入。经验：resolveJsonModule 下直接 import 大 JSON 会让 TS 推断近万行对象字面量类型，vue-tsc 变慢/易爆，?raw 是最优解。
- Dashboard.vue：统计卡片红涨绿跌；ECharts 金色折线+渐变面积；范围切换按 ISO 日期字符串比较切片（免 Date 解析）；resize 监听与 dispose 清理。
- 主 chunk 2.9MB(gzip 857KB) 因 echarts 全量打进首屏，后续可 manualChunks 优化（非阻塞项）。

## 2026-08-14 — US-004 历史数据记录页 ✅
- Market.vue：模式切换 intl/domestic/monthly；monthly 模式把 1970-2004 月度定盘价(OHLC 均取 price)与 2004-06 起国际日线拼接(按日期排序、涨跌幅按前一条收盘计算)。
- 区间筛选用 ISO 日期字符串比较；无区间时日线模式默认最近 100 条(AC 要求)，月度模式默认全量。
- 数据交叉验证：国际2020年最低1474.34(2020-03-19 疫情恐慌底)、最高2063.25(2020-08-06 历史新高)与真实历史一致，可作为后续故事验收基准。

## 2026-08-14 — US-005 金价波动分析页 ✅
- Analysis.vue：所有指标前端重算；MA 用滑动窗口增量求和 O(n)；回撤=收盘/截至当日历史最高收盘-1。
- 数据基准（可复用做验收）：近20年总涨幅+686.77%、年化+10.65%、年波动率17.30%、最大回撤-44.64%@2015-12-17（2011-09 历史高点→2015 熊市底的完整回调，与史实一致）。

## 2026-08-14 — US-006 影响黄金的全球事件库数据 ✅
- frontend/src/data/events.json：37 件事件(evt-001..evt-037)，1971-08-15 尼克松冲击 → 2025-04-22 金价破3500。
- 内容事实基准（后续 US-008 关联统计可用）：雷曼 2008-09-15 后 30 日金价下跌(流动性错杀)、2016-06-24 脱欧当日+4.8%、2020-03-19 疫情流动性底 1474、2013-04-15 两日-13%。
- schema 见 AGENTS.md；US-007 时间线页与 US-008 图上标注将直接消费本文件。

## 2026-08-14 — US-007 事件时间线页 ✅
- 新增 src/data/events.ts 加载器(?raw 模式)；Events.vue 按年份分组时间线(el-timeline)；筛选即时联动；详情对话框复用性高（US-008 的图上标注点击也会用到详情展示，届时可抽公共组件或直接复用 openDetail 逻辑）。
- 路由 /events 懒加载，菜单加"事件时间线"入口。

## 2026-08-14 — US-008 事件-金价关联标注与涨跌幅统计 ✅
- Dashboard 国际视图改用合并序列(月度1970-2004+日线2004-)，从而能标注全部37件事件；markPoint 需在 data item 里带 name 字段（echarts MarkPointDataItemOption 强制）。
- 涨跌幅基准=事件日(或其后最近交易日)收盘，前后窗口取最近可得收盘；二分查找 prev/next。
- 数据基准（验收可复用）：雷曼后365日+27.8%、2020-03-19后30日+15.0%、2016-06-24脱欧后365日-5.4%、2013-04-15暴跌前30日-15.3%。
- 共享组件 EventDetailDialog.vue 已抽离，Events.vue 同步改用。
